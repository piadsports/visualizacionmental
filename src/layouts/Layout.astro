<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link href="https://fonts.googleapis.com/css2?family=Barlow+Condensed:wght@400;600;700;800;900&family=Barlow:wght@300;400;500;600&display=swap" rel="stylesheet">
		<meta name="generator" content={Astro.generator} />
		<title>Visualización Mental — FC Sporting Company</title>
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
    :root {
        --green: #3aaa35;
        --green-dark: #2a8a26;
        --green-deeper: #1a6618;
        --green-light: #e8f7e7;
        --white: #ffffff;
        --off-white: #f7faf7;
        --gray: #6b7280;
        --gray-light: #e5e7eb;
        --text: #1a2e1a;
    }

    * { margin: 0; padding: 0; box-sizing: border-box; }

    /* 1. LAYOUT BASE: Pantalla Completa y Flexible */
    html, body {
        margin: 0;
        width: 100%;
        height: 100dvh; 
        overflow: hidden;
    }

    body {
        font-family: 'Barlow', sans-serif;
        background: var(--off-white);
        color: var(--text);
        display: flex;
        flex-direction: column; 
    }

    /* HEADER */
    .header {
        flex-shrink: 0; 
        background: var(--green);
        padding: 16px 20px 12px; 
        text-align: center;
        position: relative;
        overflow: hidden;
        transition: all 0.3s ease;
    }

    .header::before {
        content: ''; position: absolute;
        top: -40px; left: -40px; right: -40px;
        height: 100px; background: var(--green-dark);
        border-radius: 0 0 60% 60%; opacity: 0.3;
    }

    .shield-wrap { position: relative; z-index: 1; margin-bottom: 6px; }
    .shield { width: 45px; height: auto; filter: drop-shadow(0 2px 8px rgba(0,0,0,0.2)); transition: width 0.3s ease;}

    .header h1 { font-family: 'Barlow Condensed', sans-serif; font-size: 11px; font-weight: 600; letter-spacing: 2px; text-transform: uppercase; color: rgba(255,255,255,0.75); position: relative; z-index: 1; margin-bottom: 2px; }
    .header h2 { font-family: 'Barlow Condensed', sans-serif; font-size: 22px; font-weight: 900; color: var(--white); text-transform: uppercase; position: relative; z-index: 1; line-height: 1.1; margin-bottom: 2px;}
    .header p { font-size: 12px; color: rgba(255,255,255,0.8); position: relative; z-index: 1; font-weight: 400; }

    /* PROGRESS BAR */
    .progress-wrap {
        flex-shrink: 0; background: var(--white);
        padding: 10px 20px; border-bottom: 1px solid var(--gray-light); z-index: 100;
    }
    .progress-label { display: flex; justify-content: space-between; font-size: 11px; font-weight: 700; text-transform: uppercase; color: var(--gray); margin-bottom: 6px; }
    .progress-bar { height: 4px; background: var(--gray-light); border-radius: 2px; overflow: hidden; }
    .progress-fill { height: 100%; background: var(--green); border-radius: 2px; transition: width 0.4s ease; width: 0%; }

    /* CONTENEDOR DE PÁGINAS */
    .page {
        display: none;
        padding: 16px 20px 0; 
        max-width: 800px;
        width: 100%;
        margin: 0 auto;
        flex-grow: 1; 
        overflow-y: auto; 
        scrollbar-width: thin; 
    }

    .page.active { 
        display: flex; 
        flex-direction: column; 
        animation: fadeUp 0.3s ease; 
    }

    @keyframes fadeUp { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

    /* TARJETAS (Intro, Preguntas, Resultados) */
    .intro-card, .q-card, .script-card, .instructions-card { 
        background: var(--white); border-radius: 12px; 
        padding: 16px 20px; border: 1px solid var(--gray-light); margin-bottom: 12px; 
        transition: padding 0.3s ease;
    }
    
    .intro-card h3 { font-family: 'Barlow Condensed', sans-serif; font-size: 20px; font-weight: 800; text-transform: uppercase; color: var(--green-dark); margin-bottom: 8px; }
    .intro-card p { font-size: 14px; line-height: 1.5; color: var(--gray); margin-bottom: 10px; }
    .steps-list { margin-top: 12px; list-style: none; }
    .steps-list li { display: flex; align-items: center; gap: 12px; margin-bottom: 10px; font-size: 13px; color: var(--text); }
    .step-num { background: var(--green); color: white; width: 22px; height: 22px; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 11px; font-weight: 700; flex-shrink: 0; }

    .q-number { font-family: 'Barlow Condensed', sans-serif; font-size: 11px; font-weight: 700; letter-spacing: 1px; color: var(--green); margin-bottom: 4px; }
    .q-title { font-family: 'Barlow Condensed', sans-serif; font-size: 18px; font-weight: 700; line-height: 1.2; color: var(--text); margin-bottom: 4px; }
    .q-hint { font-size: 12px; color: var(--gray); margin-bottom: 12px; line-height: 1.4; }

    /* GRID DE OPCIONES */
    .options-grid { 
        display: grid; 
        grid-template-columns: 1fr;
        gap: 8px; 
    }
    .option-btn {
        display: flex; align-items: center; gap: 10px;
        padding: 10px 14px; border: 1.5px solid var(--gray-light); border-radius: 8px;
        cursor: pointer; background: var(--off-white);
        font-size: 14px; font-weight: 500; color: var(--text); text-align: left;
    }
    .option-btn.selected { border-color: var(--green); background: var(--green-light); color: var(--green-dark); }
    .option-check { width: 18px; height: 18px; border: 1.5px solid var(--gray-light); border-radius: 4px; flex-shrink: 0; display: flex; align-items: center; justify-content: center; background: white; }
    .option-btn.selected .option-check { background: var(--green); border-color: var(--green); }
    .option-check svg { opacity: 0; width: 10px; }
    .option-btn.selected .option-check svg { opacity: 1; }
    .radio-btn .option-check { border-radius: 50%; }

    /* TEXTAREAS E INPUTS */
    textarea, input[type="text"] {
        width: 100%; border: 1.5px solid var(--gray-light); border-radius: 8px;
        padding: 10px 14px; font-family: 'Barlow', sans-serif; font-size: 14px;
        color: var(--text); background: var(--off-white); resize: none;
    }
    textarea { height: 80px; }
    textarea:focus, input[type="text"]:focus { outline: none; border-color: var(--green); background: white; }
    .other-input { margin-top: 8px; display: none; }
    .other-input.visible { display: block; }
    .error-msg { color: #dc2626; font-size: 12px; margin-top: 6px; display: none; }
    .error-msg.visible { display: block; }

    /* BOTONES DE NAVEGACIÓN */
    .nav-buttons {
        display: flex; gap: 10px;
        margin-top: auto;
        padding: 16px 0;
    }
    .btn-primary, .btn-secondary, .btn-nav, .copy-btn {
        padding: 14px; border-radius: 10px; font-family: 'Barlow Condensed', sans-serif;
        font-size: 16px; font-weight: 700; text-transform: uppercase; cursor: pointer; border: none; transition: all 0.2s;
    }
    .btn-primary, .copy-btn { width: 100%; background: var(--green); color: white; box-shadow: 0 4px 10px rgba(58,170,53,0.25); }
    .btn-primary:hover, .copy-btn:hover, .btn-next:hover { background: var(--green-dark); }
    .btn-primary:active, .copy-btn:active, .btn-next:active { transform: translateY(2px); }
    .btn-secondary { width: 100%; background: transparent; color: var(--green-dark); border: 2px solid var(--green); margin-top: 8px; }
    .btn-nav { flex: 1; background: transparent; color: var(--green-dark); border: 2px solid var(--green); }
    .btn-back { flex: 0.35; } 
    .btn-next { flex: 0.65; background: var(--green); color: white; border: none; }

    /* LOADING & RESULTADOS */
    .loading-page { text-align: center; padding: 40px 20px; }
    .spinner { width: 50px; height: 50px; border: 4px solid var(--green-light); border-top-color: var(--green); border-radius: 50%; animation: spin 0.8s linear infinite; margin: 0 auto 20px; }
    @keyframes spin { to { transform: rotate(360deg); } }
    .loading-page h3 { font-family: 'Barlow Condensed', sans-serif; font-size: 20px; font-weight: 700; margin-bottom: 6px; }
    .loading-page p { font-size: 14px; color: var(--gray); }
    .loading-steps { margin-top: 24px; max-width: 220px; margin-left: auto; margin-right: auto; }
    .loading-step { display: flex; align-items: center; gap: 10px; padding: 6px 0; font-size: 13px; opacity: 0.4; transition: opacity 0.3s;}
    .loading-step.active { opacity: 1; color: var(--green-dark); font-weight: 500; }
    .loading-step.done { opacity: 0.7; }
    .loading-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--gray-light); flex-shrink: 0; }
    .loading-step.active .loading-dot { background: var(--green); }
    .loading-step.done .loading-dot { background: var(--green-dark); }

    .result-header { background: var(--green); border-radius: 12px; padding: 16px; text-align: center; margin-bottom: 12px; }
    .result-header h3 { font-family: 'Barlow Condensed', sans-serif; font-size: 12px; letter-spacing: 2px; color: rgba(255,255,255,0.7); margin-bottom: 4px;}
    .result-header h2 { font-family: 'Barlow Condensed', sans-serif; font-size: 24px; color: white; text-transform: uppercase;}
    .script-card { overflow-y: auto; max-height: 40vh; }
    .script-block { padding: 14px; border-bottom: 1px solid var(--gray-light); }
    .block-label { font-family: 'Barlow Condensed', sans-serif; font-size: 11px; color: var(--green); margin-bottom: 4px; }
    .block-text { font-size: 14px; line-height: 1.6; color: var(--text); white-space: pre-wrap; }
    .instructions-card h4 { font-family: 'Barlow Condensed', sans-serif; font-size: 15px; color: var(--green-dark); margin-bottom: 8px;}
    .instructions-card li { font-size: 12px; color: var(--green-deeper); gap: 8px; margin-bottom: 4px;}

    /* FOOTER */
    .footer {
        flex-shrink: 0; text-align: center;
        padding: 10px 20px 12px; font-size: 11px; color: var(--gray);
        background: var(--white); border-top: 1px solid var(--gray-light);
    }
    .footer span { font-weight: 600; color: var(--green-dark); }


    /* RESPONSIVE */
    @media (min-width: 768px) {
        /* Header crece */
        .header { padding: 24px 20px 20px; }
        .shield { width: 60px; margin-bottom: 12px; }
        .header h1 { font-size: 14px; }
        .header h2 { font-size: 32px; margin-bottom: 6px; }
        .header p { font-size: 16px; }

        .page.active {
            justify-content: center;
            padding: 24px 40px 0;
        }

        /* Tarjetas más amplias */
        .intro-card, .q-card, .script-card, .instructions-card { padding: 32px; }
        .q-title { font-size: 24px; margin-bottom: 8px; }
        .q-hint { font-size: 15px; margin-bottom: 20px; }

        /* Grid de opciones en 2 COLUMNAS */
        .options-grid {
            grid-template-columns: 1fr 1fr;
            gap: 16px;
        }
        .option-btn { padding: 14px 18px; font-size: 16px; }

        textarea, input[type="text"] { padding: 14px 18px; font-size: 16px; }
        textarea { height: 100px; }

        .nav-buttons {
            margin-top: 24px;
            justify-content: center; 
            gap: 20px;
        }
        .btn-nav { max-width: 250px; font-size: 18px; padding: 16px; }
        .btn-primary, .copy-btn, .btn-secondary { max-width: 400px; margin: 10px auto; font-size: 18px; padding: 16px; }

        /* Resultados */
        .result-header { padding: 24px; }
        .result-header h2 { font-size: 32px; }
        .block-text { font-size: 16px; }
        .instructions-card li { font-size: 14px; margin-bottom: 8px; }
        
        .footer { padding: 16px 20px; font-size: 13px; }
    }
</style>